import React from 'react';

import FooterComp from './FooterComp';

const Layout = ({ children }) => {
  return (
    <div>
         {children}
      {/* <FooterComp /> */}
    </div>
  );
}

export default Layout;
